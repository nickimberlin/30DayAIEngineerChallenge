# Plan: README Improvements for AI Proposal Generator

## Goal
Enhance the README with all suggested improvements: screenshot placeholders, design system reference, model performance section, architecture diagram, roadmap, user workflow, local AI comparison, and FAQ.

## Changes to Make

### 1. Screenshot/Mockup Placeholders
- Add a hero image placeholder at the top (below the tagline)
- Add a "Screenshots" section with placeholder images for:
  - Dashboard view
  - Proposal editor
  - PDF export preview
  - Model loading/progress screen

### 2. Design System Reference
- Add a "Design System" section after Tech Stack
- Summarize the Wise-inspired theme (colors, typography, components)
- Link to `design.md` for full details
- Include the key color swatches as a visual table

### 3. Model Performance Section
- Add a "Model Performance" section after AI Architecture
- Include a table with:
  - Model name, approximate download size, typical inference time, use case
  - Gemma 4, E2B, E4B rows
- Browser compatibility matrix (Chrome, Edge, Firefox, Safari with WebGPU/WASM notes)
- Hardware recommendations (RAM, GPU)

### 4. Architecture Diagram (ASCII)
- Add an "Architecture" section with ASCII art showing:
  - Browser (Transformers.js + WebGPU/WASM) → Model Cache (IndexedDB/Cache API)
  - Frontend (Next.js) ↔ Backend (FastAPI) for non-AI operations
  - PDF generation flow
  - Clear visual showing AI runs entirely client-side

### 5. Roadmap Section
- Add a "Roadmap" section after Monetization
- Three phases:
  - **Phase 1 (MVP)**: Core proposal generation, single template, PDF export, basic branding
  - **Phase 2 (v2)**: Multi-template, client management, version history, offline mode
  - **Phase 3 (v3)**: E-signatures, email integration, team collaboration, API access

### 6. User Workflow Section
- Add a "How It Works" section after Features
- Step-by-step flow:
  1. Enter project prompt
  2. AI generates proposal sections
  3. Review & edit generated content
  4. Customize branding and pricing
  5. Export as PDF
- Include as a numbered visual flow

### 7. Why Local AI Section
- Add a "Why Local AI?" section after AI Architecture
- Comparison table: Local AI vs Cloud AI
  - Rows: Cost, Privacy, Latency, Offline, Vendor Lock-in, Scalability
- Highlight advantages: zero per-token costs, data never leaves browser, no API rate limits, works offline

### 8. FAQ Section
- Add a "FAQ" section before Contributing
- Questions:
  - How large are the model downloads?
  - What browsers are supported?
  - Can I use this without an internet connection?
  - How does this compare to ChatGPT/Claude for proposals?
  - Is my data sent to any server?
  - What if my device doesn't support WebGPU?
  - Can I use custom/fine-tuned models?

## File to Modify
- `/Users/casper/Documents/30DayAIEngineerChallenge/Day01-Proposal-Generator/README.md`

## Ordering of New Sections
1. Overview (existing)
2. Use Cases (existing)
3. How It Works (NEW - user workflow)
4. Features (existing)
5. Screenshots (NEW - placeholders)
6. Tech Stack (existing)
7. AI Architecture (existing)
8. Why Local AI? (NEW - comparison)
9. Model Performance (NEW - sizes, speeds, compatibility)
10. Architecture (NEW - ASCII diagram)
11. Design System (NEW - summary + link to design.md)
12. Project Structure (existing)
13. Getting Started (existing)
14. API Endpoints (existing)
15. Roadmap (NEW)
16. Monetization Strategy (existing)
17. Portfolio Value (existing)
18. FAQ (NEW)
19. Contributing (existing)
20. License (existing)
21. Author (existing)
